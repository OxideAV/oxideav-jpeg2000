# Test fixtures are binary — never convert line endings on checkout.
# PGM (P5) and J2K files contain a raw byte stream where bare 0x0A or
# 0x0D bytes are valid pixel values; autocrlf would corrupt them on
# Windows CI runners.
tests/fixtures/** binary
